# Server
PORT=3000
NODE_ENV=development

# Authentication
API_KEY=dev-api-key-123

# LLM Configuration
# Options: "mock" (default, no API key needed) | "anthropic" (requires ANTHROPIC_API_KEY)
LLM_MODE=mock

# Anthropic (optional — only needed if LLM_MODE=anthropic)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514
